<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>파일 상세보기</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #dee2e6;
      font-family: 'Georgia', serif;
    }

    .yugioh-card {
      width: 300px;
      height: auto;
      background: linear-gradient(to bottom, #c69c6d, #6e4f30);
      border: 8px solid #212529;
      border-radius: 12px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.9);
      margin: auto;
      padding: 16px;
      color: #fff8dc;
      text-align: center;
    }

    .card-number {
      font-size: 1rem;
      font-weight: bold;
      background-color: #fff8dc;
      color: #2b1e09;
      padding: 4px 12px;
      border-radius: 8px;
      width: fit-content;
      margin: 0 auto 10px;
    }

    .card-title {
      font-weight: bold;
      font-size: 1.3rem;
      background-color: #6c757d;
      padding: 10px;
      border-radius: 10px;
      border: 2px solid #212529;
      margin-bottom: 12px;
    }

    .file-list {
      margin-top: 10px;
      font-size: 0.9rem;
      text-align: left;
      color: #fff8dc;
    }

    .file-item a {
	  text-decoration: none;  /* 🔥 밑줄 제거 */
	  color: #fff8dc;         /* 카드에 어울리는 색상 */
	  word-break: break-all;  /* 긴 이름 줄바꿈 */
	  display: inline-block;
	  max-width: 250px;
	}

    .btn-area a {
      margin: 5px;
    }

    .msg-box {
      color: red;
      text-align: center;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="container py-4">

    <h1 class="text-center mb-4">🃏 카드 상세보기</h1>

    {{#msg}}
    <div class="msg-box">{{msg}}</div>
    {{/msg}}

    <div class="text-center mb-3">
      <a href="/boardList" class="btn btn-dark btn-sm">🏠 목록으로</a>
    </div>

    <div class="yugioh-card">
      <div class="card-number">No. {{boardMapping.bno}}</div>

      <div class="card-title">{{boardMapping.title}}</div>

      <div class="btn-area">
        <a href="/modifyBoard?bno={{boardMapping.bno}}" class="btn btn-outline-light btn-sm">✏️ 수정</a>
        <a href="/removeBoard?bno={{boardMapping.bno}}" class="btn btn-outline-danger btn-sm">🗑️ 삭제</a>
      </div>

      <div class="file-list mt-3">
        <h6 class="text-light">📎 첨부파일</h6>
        {{#fileList}}
        <div class="file-item mb-1">
          🔗 <a href="/upload/{{fname}}.{{fext}}" download="{{foriginname}}">{{foriginname}}</a>
          <a href="/removeBoardfile?fno={{fno}}&bno={{bno}}" class="btn btn-danger ms-2">삭제</a>
        </div>
        {{/fileList}}
      </div>
    </div>
  </div>
</body>
</html>