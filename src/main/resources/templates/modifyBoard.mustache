<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>게시글 수정</title>
<!-- 부트스트랩 스타일 적용 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
	<h3 class="text-center mb-4">게시글 수정</h3>
	<form action="/modifyBoard" method="post" class="card p-4 shadow" style="max-width: 500px; margin: auto;">
	
	<!--  게시글 번호는 숨김을 전달 -->
	<input type="hidden" name="bno" value="{{bno}}">
	
	<!-- 제목 수정 입력 -->
	<div class="mb-3">
		<label for="title" class="form-label">제목</label>
		<input type="text" class="form-control" name="title" value="{{title}}" required>
	</div>
	
	<!-- 비밀번호 입력 -->
	<div class="mb-3">
		<label for="pw" class="form-label">비밀번호</label>
		<input type="password" class="form-control" name="pw" plascehler="비밀번호 입력" required>
	</div>
	
	<!-- 수정 버튼 -->
	<button type="submit" class="btn btn-warnint w-100">제목 수정</button>
	
	<!-- 에러 메세지 표시 영역 -->
	{{#msg}}
		<div class="alert alert-danger mt-3">{{msg}}</div>
	{{/msg}}
	</form>
</body>
</html>