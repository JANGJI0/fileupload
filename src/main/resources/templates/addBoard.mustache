<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>게시글 작성</title>
  <!-- 부트스트랩 스타일 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <h3 class="text-center my-4">📝 게시글 작성</h3>

  <form method="post" action="/addBoard" enctype="multipart/form-data" class="card p-4 shadow" style="max-width: 500px; margin: auto;">

    <!-- 제목 입력 -->
    <div class="mb-3">
      <label for="title" class="form-label">제목</label>
      <input type="text" class="form-control" name="title" placeholder="제목 입력" required>
    </div>

    <!-- 비밀번호 입력 -->
    <div class="mb-3">
      <label for="pw" class="form-label">비밀번호</label>
      <input type="password" class="form-control" name="pw" placeholder="비밀번호 입력" required>
    </div>

    <!-- 파일 업로드 -->
    <div class="mb-3">
      <label for="fileList" class="form-label">첨부파일</label>
      <input type="file" class="form-control" name="fileList" multiple>
      <div class="form-text">10MB 이하 파일만 첨부 가능합니다.</div>
    </div>

    <!-- 저장 버튼 -->
    <button type="submit" class="btn btn-primary w-100">저장</button>

  </form>

</body>
</html>